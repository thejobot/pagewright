{
  "canonical": "functions/ai/chat.js",
  "tools": [
    {
      "name": "search_repo",
      "purpose": "GitHub code search across the configured repo",
      "input": { "q": "string" }
    },
    {
      "name": "read_repo_file",
      "purpose": "Read one file from the configured repo",
      "input": { "path": "string" }
    },
    {
      "name": "wire_element",
      "purpose": "Propose an onclick/handler snippet for an interactive element",
      "input": { "intent": "string", "element_id": "string (optional)" }
    },
    {
      "name": "publish_page",
      "purpose": "Commit exported HTML to the configured publish path and open a PR",
      "input": { "slug": "string", "html": "string" }
    }
  ]
}
